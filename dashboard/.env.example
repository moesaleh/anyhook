# URL of the AnyHook backend API (subscription-management service).
# This is a build-time variable — Next.js inlines it into the client bundle,
# so it MUST be set when running `next build`, not just at runtime.
NEXT_PUBLIC_API_URL=http://localhost:3001
